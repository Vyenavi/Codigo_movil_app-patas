<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.apppatas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".encontrar.TeEncontreActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".Inicio">

        </activity>
        <activity android:name=".perfil.mascota.PerfilMascotaActivity" />
        <activity android:name=".perfil.usuario.PerfilUsuarioActivity" />
        <activity android:name=".perfil.mascota.vista.VistaMascotaActivity"></activity>
        <activity android:name=".publicar.PublicarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>